name: 'taxon-labels'

metadata:
  description: 'Lookup file for NCBITaxon labels generated from Phenio'

reader:
  format: 'csv'
  files:
    - '../../../data/monarch/taxon_labels.tsv'
  delimiter: '\t'
  columns:
    - 'id'
    - 'label'

transform:
  key: 'id'
  values:
    - 'label'

