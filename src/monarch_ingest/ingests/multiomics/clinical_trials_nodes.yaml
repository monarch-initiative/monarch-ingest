name: 'multiomics_clinical_trials'

files:
  - './data/multiomics-kp/clinical_trials_kg_v2.7.2_nodes.tsv'

# Format options csv, json
format: 'csv'

# Specify a delimiter for csv formats
delimiter: '\t'

global_table: './src/monarch_ingest/translation_table.yaml'

transform_code: './src/monarch_ingest/ingests/multiomics/clinical_trials_nodes.py'

# For a csv/tsv file, list expected columns
columns:
  - id
  - name
  - category

node_properties:
  - 'id'
  - 'name'
  - 'category'