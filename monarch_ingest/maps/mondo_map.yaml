name: 'mondo_map'

metadata:
  description: 'Mapping file from unmapped EBI Gene-Phenotype disease names to MONDO terms'

format: 'csv'

delimiter: '\t'

#  https://storage.googleapis.com/monarch-ingest/manually-uploaded-data/unmapped_ebi_diseases.tsv
files:
  - './data/ebi/unmapped_ebi_diseases.tsv'

header: 0

columns:
  - 'name'
  - 'id'

key: 'name'

values:
  - 'id'
